server:
  port: 8766
security.basic.enabled: false
turbine:
  aggregator:
    clusterConfig: default
  appConfig: user-service , blog-service   #配置需要监控的服务名
  clusterNameExpression: new String("default")

management:
  security:
    enabled: false